@model IEnumerable<Mercedes_Matriz_de_Conhecimento.tblPerfilTreinamento>


<!DOCTYPE html>

<script type="text/javascript">

</script>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <h2 class="text-center">Perfil de Treinamento</h2>
    <div class="container table-place">

        <table class="table table-striped">
            <thead class="text-center">
                <tr class="table-dark">
                    <th class="text-center">Nome</th>
                    <th class="text-center">Usuário Criação</th>
                    <th class="text-center"></th>
                </tr>
            </thead>
            <tbody>

                @foreach (var training in Model)
                {
                    <tr class="text-center">
                        <td>@training.Nome</td>
                        <td>@training.UsuarioCriacao</td>
                        <td>
                            <div class="btn-group" role="group">
                                <a class="btn btn-warning btn-group-sm" href="@Url.Action("Details","TrainingProfile",new { id = @training.IdPerfilTreinamento })">Editar</a>
                                <a class="btn btn-default btn-group-sm" href="@Url.Action("Delete","TrainingProfile",  new { id = @training.IdPerfilTreinamento})">Excluir</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <hr />
        <a type="button" class="btn btn-primary btn-group-sm pull-right" href="@Url.Action("Create", "TrainingProfile")">
            Cadastrar Perfil de Treinamento
        </a>
    </div>

</body>
</html>
