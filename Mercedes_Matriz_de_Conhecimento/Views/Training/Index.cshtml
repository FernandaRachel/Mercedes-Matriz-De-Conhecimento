@model IEnumerable<Mercedes_Matriz_de_Conhecimento.tblWorkzone>


<!DOCTYPE html>

<script type="text/javascript">

</script>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <h2 class="text-center">Workzone</h2>
    <div class="container table-place">

        <table class="table table-striped">
            <thead class="text-center">
                <tr class="table-dark">
                    <th class="text-center">Nome</th>
                    <th class="text-center">Descrição</th>
                    <th class="text-center">Pessoas Necessárias</th>
                    <th class="text-center">Ativo</th>
                    <th class="text-center"></th>
                </tr>
            </thead>
            <tbody>

                @foreach (var workzone in Model)
                {
                    <tr class="text-center">
                        <td>@workzone.Nome</td>
                        <td>@workzone.Descricao</td>
                        <td>@workzone.PessoasNecessarias</td>
                        @if (@workzone.FlagAtivo)
                        {
                            <td>Ativada</td>
                        }
                        else
                        {
                            <td>Desativada</td>
                        }
                        <td>
                            <div class="btn-group" role="group">
                                <a class="btn btn-warning btn-group-sm" href="@Url.Action("Details","Workzone",new { id = @workzone.IdWorkzone })">Editar</a>
                                <a class="btn btn-default btn-group-sm" href="@Url.Action("Delete","Workzone",  new { id = @workzone.IdWorkzone})">Excluir</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <hr />
        <a type="button" class="btn btn-primary btn-group-sm pull-right" href="@Url.Action("Create", "Workzone")">
            Cadastrar Workzone
        </a>
    </div>

</body>
</html>
