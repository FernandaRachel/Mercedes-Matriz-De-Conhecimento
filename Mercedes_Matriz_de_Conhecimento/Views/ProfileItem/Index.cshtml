@model IEnumerable<Mercedes_Matriz_de_Conhecimento.tblPerfilItem>


<!DOCTYPE html>

<script type="text/javascript">

</script>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <h2 class="text-center">Itens</h2>
    <div class="container table-place">

        <table class="table table-striped">
            <thead class="text-center">
                <tr class="table-dark">
                    <th class="text-center">Sigla</th>
                    <th class="text-center">Descrição</th>
                    <th class="text-center"></th>
                </tr>
            </thead>
            <tbody>

                @foreach (var ProfileItem in Model)
                {
                    <tr class="text-center">
                        <td>@ProfileItem.Sigla</td>
                        <td>@ProfileItem.Descricao</td>
                        <td>
                            <div class="btn-group" role="group">
                                <a class="btn btn-warning btn-group-sm" href="@Url.Action("Details","ProfileItem",new { id = @ProfileItem.IdPerfilItem})">Editar</a>
                                <a class="btn btn-default btn-group-sm" href="@Url.Action("Delete","ProfileItem",  new { id = @ProfileItem.IdPerfilItem})">Excluir</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <hr />
        <a type="button" class="btn btn-primary btn-group-sm pull-right" href="@Url.Action("Create", "ProfileItem")">
            Cadastrar Item
        </a>
    </div>

</body>
</html>
